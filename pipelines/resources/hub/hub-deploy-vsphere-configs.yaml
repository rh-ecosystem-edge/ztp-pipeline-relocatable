apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: hub-deploy-vsphere-configs
  annotations:
    description: |
      This task will deploy RHACM on the Hub Cluster.
spec:
  workspaces:
    - name: ztp
  params:
    - name: ztp-container-image
      type: string
      default: "quay.io/takinosh/ztpfw-pipeline:dev"
    - name: kubeconfig
      type: string
      default: ""
    - name: spokes-config
      type: string
      default: ""
    - name: mock
      type: string
      default: "false"
  stepTemplate:
    env:
      - name: WORKDIR
        value: "/workspace/ztp"
      - name: SPOKES_CONFIG
        value: $(params.spokes-config)
      - name: KUBECONFIG
        value: "$(workspaces.ztp.path)/kubeconfig"
      - name: DEPLOY_VMWARE_DIR
        value: "deploy-vsphere-configs"
      - name: SHARED_DIR
        value: "shared-utils"
      - name: MOCK
        value: $(params.mock)
  steps:
    - name: deploy-vsphere-configs
      image: "$(params.ztp-container-image)"
      imagePullPolicy: Always
      script: |
        #!/usr/bin/bash

        if [[ "${MOCK}" == 'false' ]]; then
          cd ${WORKDIR}/${DEPLOY_VMWARE_DIR}
          ./deploy.sh
        else
          echo "Deploy VSphere Configs: Mock mode on"
        fi

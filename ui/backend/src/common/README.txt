This code is shared with frontend.
Let's move that into a real library, if ever really needed.

Frintend build makes a copy of that as a hack to avoid either creating a library out of that or rewiring react-scripts there.

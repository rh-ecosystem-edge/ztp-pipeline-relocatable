{
  "name": "backend",
  "version": "0.0.1",
  "description": "The UI backend for the KubeFrame project.",
  "main": "src/index.js",
  "author": "Marek Libra",
  "license": "Apache-2.0",
  "scripts": {
    "start": "ts-node-dev --no-deps --respawn --transpile-only src/index.ts",
    "build": "tsc --sourceMap false --declaration false",
    "clean": "rm -rf node_modules build"
  },
  "dependencies": {
    "express": "^4.17.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.10",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.5"
  }
}
